// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

//TERRIBLY UNSECURE!!!! Good thing this is just a proof of concept project! :S
datasource postgresql {
  provider = "postgresql"
  url      = env("postgresql://uniplanner:OT7e|zdIUiCq2!@34.47.10.230:5432/Uniplanner?sslmode=verify-full&sslcert=/var/www/ssl/SQL/client-cert.pem&sslkey=/var/www/ssl/SQL/client-key.pem&sslrootcert=/var/www/ssl/SQL/server-ca.pem")
}

datasource mongodb {
  provider = "mongodb"
  url      = env("mongodb+srv://uniplanner:8pTPPqv3g6kjFbiW@uniplanner-cluster.m1kmo.mongodb.net/")
}

model Course {
  cid        Int      @id @default(autoincrement())
  coursenum  String
  coursename String
  credits    Int
  iselective Boolean
  prereq     Int[]
}

